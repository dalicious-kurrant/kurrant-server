# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-gradle

name: 상용 메이커스 배포

on:
  push:
    branches: [ "release-makers" ]

permissions:
  contents: read

jobs:
  build-image:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: JDK 17 세팅
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'adopt'

    # app-makers-api의 설정파일(.yaml) 생성
    - name: app-makers-api application.yaml(prod) 생성
      run: |
        cd ./app-makers-api/src/main
        if [ ! -d resources ]; then
        mkdir resources
        fi
        cd resources
        touch ./application.yaml
        echo "${{ secrets.APP_MAKERS_API_PROD_YAML }}" > ./application.yaml
        touch ./application-prod.yaml
        echo "${{ secrets.APP_MAKERS_API_PROD_PROD_YAML }}" > ./application-prod.yaml
      shell: bash

    - name: client-core application-jwt.properties 생성
      run: |
        cd ./client-core/src/main
        if [ ! -d resources ]; then
        mkdir resources
        fi
        cd resources
        touch ./application-jwt.properties
        echo "${{ secrets.CLIENT_CORE_JWT_PROPERTIES }}" > ./application-jwt.properties
      shell: bash

    - name: client-external-mail application-mail.properties 생성
      run: |
        cd ./client-external-mail/src/main
        if [ ! -d resources ]; then
        mkdir resources
        fi
        cd resources
        touch ./application-mail.properties
        echo "${{ secrets.CLIENT_EXTERNAL_MAIL_PROPERTIES }}" > ./application-mail.properties
      shell: bash

    - name: client-external-sms application-mail.properties 생성
      run: |
        cd ./client-external-sms/src/main
        if [ ! -d resources ]; then
        mkdir resources
        fi
        cd resources
        touch ./application-sms.properties
        echo "${{ secrets.CLIENT_EXTERNAL_SMS_PROPERTIES }}" > ./application-sms.properties
      shell: bash

    - name: data-redis application-redis.yml 생성
      run: |
        cd ./data-redis/src/main
        if [ ! -d resources ]; then
        mkdir resources
        fi
        cd resources
        touch ./application-redis.properties
        echo "${{ secrets.DATA_REDIS_REDIS_PROPERTIES }}" > ./application-redis.properties
      shell: bash

    - name: domain-payment application-payment.properties 생성
      run: |
        cd ./domain-payment/src/main
        if [ ! -d resources ]; then
        mkdir resources
        fi
        cd resources
        touch ./application-payment.properties
        echo "${{ secrets.DOMAIN_PAYMENT_PROD_PROPERTIES }}" > ./application-payment.properties
      shell: bash

    - name: domain-file application-s3.properties 생성
      run: |
        cd ./domain-file/src/main
        if [ ! -d resources ]; then
        mkdir resources
        fi
        cd resources
        touch ./application-s3.properties
        echo "${{ secrets.DOMAIN_FILE_S3_PROPERTIES }}" > ./application-s3.properties
      shell: bash
      
    - name: system-core application-alimtalk.properties 생성
      run: |
        cd ./system-core/src/main
        if [ ! -d resources ]; then
        mkdir resources
        fi
        cd resources
        touch ./application-alimtalk.properties
        echo "${{ secrets.SYSTEM_CORE_ALIMTALK }}" > ./application-alimtalk.properties
      shell: bash

    - name: client-alarm application-map.properties 생성
      run: |
        cd ./domain-address/src/main
        if [ ! -d resources ]; then
        mkdir resources
        fi
        cd resources
        touch ./application-map.properties
        echo "${{ secrets.SYSTEM_CORE_MAP }}" > ./application-map.properties
      shell: bash

    - name: gradlew 실행 권한 추가
      run: chmod +x gradlew

    - name: Gradle 빌드
      run: ./gradlew build

    - name: Docker 빌드
      run: |
        docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
        cd app-makers-api
        docker build -t makers_kurrant_v1 .
        docker tag makers_kurrant_v1 ${{ secrets.DOCKER_USERNAME }}/makers_kurrant_v1:${GITHUB_SHA::7}
        docker push ${{ secrets.DOCKER_USERNAME }}/makers_kurrant_v1:${GITHUB_SHA::7}


    - name: 배포
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.PROD_HOST }}
        username: ubuntu
        key: ${{ secrets.PROD_PRIVATE_KEY }}
        envs: GITHUB_SHA
        script: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
          docker pull ${{ secrets.DOCKER_USERNAME }}/makers_kurrant_v1:${GITHUB_SHA::7}
          docker tag ${{ secrets.DOCKER_USERNAME }}/makers_kurrant_v1:${GITHUB_SHA::7} makers_kurrant_v1
          docker stop makers_kurrant_v1
          sleep 5
          docker run -i -t --log-driver=awslogs --log-opt awslogs-region=ap-northeast-2 --log-opt awslogs-group=kurrant/prod --log-opt awslogs-stream=makers -d --rm --net redis_redis_cluster --name makers_kurrant_v1 -p 8884:8884 makers_kurrant_v1