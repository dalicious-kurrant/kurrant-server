package co.kurrant.app.public_api.service;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class SellServiceTest {
  //
  // @Autowired
  // GradeRepository gradeRepository;
  // @Autowired
  // SellService sellService;
  // @Autowired
  // SellRepository sellRepository;
  //
  // @Nested
  // @DisplayName("성공 테스트")
  // class SuccessTest {
  //
  // @Test
  // @Transactional
  // @DisplayName("판매 게시글 등록")
  // void CreateSell() {
  // // given
  // //
  // // Grade grade = Grade.builder().nameKo("temp1").detailModel(null).build();
  // // gradeRepository.save(grade);
  // //
  // // SellCreateRequestDto sellCreateRequestDto =
  // // SellCreateRequestDto.builder().carNumber("차량번호")
  // // .gradeId(grade.getId()).releaseDate("출고일").mileage(0).color("색상").drivingSystem("구동방식")
  // // .autoTransmission("변속기").fuelType("연료").phoneNumber("휴대폰번호").location1("시").location2("구")
  // // .hasAccident("사고이력").accidentDetail("사고이력 상세").needRepair("차량상태").needRepairDetail("차량상태
  // // 상세")
  // // .buyingType("기타상태").contactType("차량명의구분").ownerName("차량 소유주 이름").noPhoto(true).build();
  // // // when
  // // ResponseDto<CreateResponseDto<BigInteger>> result =
  // // sellService.createSell(sellCreateRequestDto);
  // //
  // // Optional<Sell> result2 = sellRepository.findById(result.getData().getKey());
  // //
  // // // then
  // // assertThat(result2.get().getGrade().getId()).isEqualTo(grade.getId());
  // }
  // }
}
